- setting PYTHONHASHSEED to the fixed seed number.
- renaming a few variables to make them more distinguishable from each other.
- adding early stop on validation set with a patience number.
- adding support for all models available in an inference exemplar script, merging the inference script specifically designed for bert-based models.
- refining implementations of available models.
- adding a graph-based model ASGCN.

- ASGCN tested
- train tested
- train cross-val tested
- infer example tested

- bert-based models are not tested